apiVersion: v1
kind: Service
metadata:
  name: {{ include "run-once.fullname" . }}
  labels:
    {{- include "run-once.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
    {{- include "run-once.selectorLabels" . | nindent 4 }}
