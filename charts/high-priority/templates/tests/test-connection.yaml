apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "high-priority.fullname" . }}-test-connection"
  labels:
    {{- include "high-priority.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "high-priority.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
