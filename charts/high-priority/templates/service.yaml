apiVersion: v1
kind: Service
metadata:
  name: {{ include "high-priority.fullname" . }}
  labels:
    {{- include "high-priority.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
    {{- include "high-priority.selectorLabels" . | nindent 4 }}
